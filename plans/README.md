# Implementation Plans

Generated by the `improve` skill on 2026-07-10 against commit `00e51f5`. The user selected every vetted finding and product-direction opportunity. Execute in the order below unless dependencies allow safe parallel review. Each plan is one contextual commit with tests and documentation; read the full plan before changing source and update its row when complete.

All build, test, migration and runtime commands must run inside the dev container. Detect `podman compose` first and fall back to `docker compose` exactly as documented in `AGENTS.md`.

## Execution order and status

| Plan | Title                            | Priority | Effort | Depends on    | Commit message                                             | Status |
| ---- | -------------------------------- | -------: | -----: | ------------- | ---------------------------------------------------------- | ------ |
| 001  | Repair migration ledger          |       P0 |      M | —             | `fix(db): repair skipped MFA migration`                    | DONE   |
| 002  | Stop password echo               |       P0 |      S | —             | `fix(auth): exclude secrets from action responses`         | DONE   |
| 003  | Expand coverage scope            |       P1 |      M | 001, 002      | `test: enforce complete server coverage`                   | DONE   |
| 004  | Enforce i18n                     |       P1 |      M | 003           | `test(i18n): enforce user-visible translation coverage`    | DONE   |
| 005  | Bound money values               |       P1 |    S–M | 001, 004      | `fix(money): enforce safe amount boundaries`               | DONE   |
| 006  | Atomic audit events              |       P1 |      L | 003           | `refactor: make audit events transactionally consistent`   | DONE   |
| 007  | Preaggregate usage counts        |       P1 |      M | 003           | `perf(db): preaggregate category and catalog usage`        | DONE   |
| 008  | Batch import writes              |       P2 |      M | 003, 005      | `perf(imports): batch duplicate checks and inserts`        | DONE   |
| 009  | Durable invitation delivery      |       P1 |      L | 001, 003, 006 | `feat(invitations): add durable email delivery`            | DONE   |
| 010  | Recoverable attachment lifecycle |       P1 |      L | 001, 003, 006 | `fix(storage): make attachment lifecycle recoverable`      | DONE   |
| 011  | Validate request IDs             |       P2 |      S | 003           | `fix(observability): validate external request ids`        | DONE   |
| 012  | Import preview and undo          |       P2 |      L | 004, 005, 008 | `feat(imports): add preview and guarded undo`              | DONE   |
| 013  | OFX reconciliation               |       P3 |      L | 005, 006, 012 | `feat(reconciliation): match OFX transactions to expenses` | DONE   |
| 014  | Budget-alert controls            |       P3 |      L | 004, 006, 009 | `feat(budgets): add alert controls and delivery history`   | DONE   |
| 015  | Expense trash                    |       P3 |      L | 006, 010      | `feat(expenses): add recoverable trash`                    | DONE   |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED — <reason>`, `REJECTED — <reason>`.

## Dependency notes

- Plan 001 is the emergency existing-database repair and must land before any later migration.
- Plan 003 establishes honest coverage before large service refactors.
- Plan 004 prevents new feature UI/error strings from reopening translation gaps.
- Plan 005 defines amount boundaries before import/reconciliation work.
- Plans 006–011 are independent after their listed foundations and should remain separate commits.
- Plan 012 builds the server-authoritative staging model reused by plan 013.
- Plan 010 must land before plan 015 because current attachment deletion is not reversible.

## Final integrated validation

After all plans land, use the selected Compose runtime for the entire session and run:

1. `pnpm db:migrate` twice.
2. `pnpm test:migrations`.
3. `pnpm verify`.
4. Run every Playwright project documented in `docs/development.md` and retain diagnostics only for failures.
5. Use the collaborative browser to exercise English and pt-BR flows at desktop and mobile widths: authentication, workspace creation, categories/catalogs, expense CRUD/review/payment/attachments/trash, budgets/alerts, imports/undo/reconciliation, invitations/roles/MFA, reports/export and audit.
6. Restore a disposable backup and run the recovery drill including attachment checksum validation.
7. Confirm `git diff --check`, clean status, one contextual commit per plan and no schema diff after the final migration snapshot.

## Findings considered and rejected

- Restricting member/viewer access to Users and Audit screens was not planned: `roles.e2e.ts` explicitly characterizes every role as able to read shared screens, while mutations remain role-protected. Changing that is a product-policy decision, not a demonstrated regression.
- Returning HTTP 200 while a background job is degraded was not planned: `docs/operations.md` explicitly documents monitoring the JSON state rather than restarting otherwise healthy web processes.
- Dependency upgrades were not planned: the current production audit has no high or critical advisories; existing low/moderate exceptions are documented in `docs/security.md`.
