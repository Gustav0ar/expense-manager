export const maxAttachmentBytes = 2 * 1024 * 1024;
export const imageAttachmentCompressionThresholdBytes = 512 * 1024;
export const imageAttachmentCompressionTargetBytes = Math.floor(maxAttachmentBytes * 0.92);
